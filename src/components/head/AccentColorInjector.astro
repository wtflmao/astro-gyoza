<script>
  import chroma from 'chroma-js'
  import { color as themeColor } from '@/config.json'

  function pickRandomAccent() {
    const seed = (Math.random() * themeColor.accent.length) | 0
    return themeColor.accent[seed]
  }

  function getRgbVal(color: chroma.Color) {
    return color.rgb().join(' ')
  }

  function injectColor() {
    const accentColor = pickRandomAccent()

    const rootBgColor = {
      light: chroma.mix('rgb(250,250,250)', accentColor.light, 0.05, 'rgb'),
      dark: chroma.mix('rgb(0,2,18)', accentColor.dark, 0.12, 'rgb'),
    }

    const $style = document.createElement('style')
    $style.textContent = `html {
  --color-accent: ${getRgbVal(chroma(accentColor.light))};
  --color-bg-root: ${getRgbVal(rootBgColor.light)};
  --color-bg-primary: ${getRgbVal(chroma(themeColor.accent[0].light))};
  --color-bg-secondary: ${getRgbVal(chroma(themeColor.accent[0].light))};
  --color-text-primary: ${getRgbVal(chroma(themeColor.accent[1].light))};
  --color-text-secondary: ${getRgbVal(chroma(themeColor.accent[1].light))};
  --color-border-primary: ${getRgbVal(chroma(themeColor.accent[2].light))};
}
[data-theme='dark'] {
  --color-accent: ${getRgbVal(chroma(accentColor.dark))};
  --color-bg-root: ${getRgbVal(rootBgColor.dark)};
  --color-bg-primary: ${getRgbVal(chroma(themeColor.accent[0].dark))};
  --color-bg-secondary: ${getRgbVal(chroma(themeColor.accent[0].dark))};
  --color-text-primary: ${getRgbVal(chroma(themeColor.accent[1].dark))};
  --color-text-secondary: ${getRgbVal(chroma(themeColor.accent[1].dark))};
  --color-border-primary: ${getRgbVal(chroma(themeColor.accent[2].dark))};
}`
    document.head.appendChild($style)
  }

  injectColor()

  document.addEventListener('swup:content:replace', injectColor)
</script>
